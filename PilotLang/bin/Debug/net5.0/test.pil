enum e{
    var1,
    var2,
}

enum e2{
    var1(t),
    var2(c)
}

enum e3<T>{
    var1(T),
    var2(T)
}

fn int Main() {
    
    var sum int = 0;
    for i < 15 {
        sum++;
    }
    
    
    
    return 0;
}